"""
Regressions over acquisitions that are not in the repository.

Skipped unless asked for — `OPENQUANT_REAL_DATA=1` or `-m real` — and each
test skips itself, naming the path, when its own files are absent. See
`README.md` beside this file, and `data.py` for where each set is looked for.
"""
